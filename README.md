# Selenium UI Automation Framework

**Tech stack:** Java, Selenium WebDriver, TestNG, Maven

## Features
- Page Object Model (POM)
- Reusable utilities for waits, browser actions, and assertions
- TestNG test suites with parallel execution support

## How to run
1. Clone the repository
2. Import as Maven project
3. Run tests with:
   ```bash
   mvn clean test
